This project can easily be build using Maven. Just run:

> mvn package

This will create a war file in the "target" directory. Deploy that
to a servlet container, start it up and navigate to the root context
URL (probably something like http://localhost:8080/hibernate-maven-web-0.5.0/).